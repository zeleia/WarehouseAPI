name: .NET

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
        
    - name: Restore standard dependencies
      run: dotnet restore
      
    - name: Build standard API
      run: dotnet build --no-restore
      
    - name: Run controller tests
      run: dotnet test -p:IsTesting=true --verbosity normal